apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "game-application.fullname" . }}-test-connection"
  labels:
    {{- include "game-application.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "game-application.fullname" . }}:{{ .Values.service.port }}']
