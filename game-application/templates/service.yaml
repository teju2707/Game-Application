apiVersion: v1
kind: Service
metadata:
  name: {{ include "game-application.fullname" . }}
  labels:
    {{- include "game-application.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "game-application.selectorLabels" . | nindent 4 }}
